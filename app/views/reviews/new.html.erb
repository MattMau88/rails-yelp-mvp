<h1>Add a new review</h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, hint: 'Rating must be integer between 1 - 5' %>
  <%= f.button :submit, class: "btn btn-primary"%>
<% end %>
